diff --git a/assets/js/RepLog/RepLogApp.js b/assets/js/RepLog/RepLogApp.js
index caddd034..da47e649 100644
--- a/assets/js/RepLog/RepLogApp.js
+++ b/assets/js/RepLog/RepLogApp.js
@@ -13,7 +13,8 @@ export default class RepLogApp extends Component {
         this.state = {
             highlightedRowId: null,
             repLogs: [],
-            numberOfHearts: 1
+            numberOfHearts: 1,
+            isLoaded: false
         };
 
         this.handleRowClick = this.handleRowClick.bind(this);
@@ -25,7 +26,8 @@ export default class RepLogApp extends Component {
         getRepLogs()
             .then((data) => {
                 this.setState({
-                    repLogs: data
+                    repLogs: data,
+                    isLoaded: true
                 })
             });
     }
@@ -70,7 +72,7 @@ export default class RepLogApp extends Component {
     }
 
     render() {
-        const { highlightedRowId, repLogs } = this.state;
+        const { highlightedRowId, repLogs, isLoaded } = this.state;
 
         let heart = '';
         if (this.props.withHeart) {
@@ -103,6 +105,7 @@ export default class RepLogApp extends Component {
                         handleRowClick={this.handleRowClick}
                         handleDeleteRepLog={this.handleDeleteRepLog}
                         repLogs={repLogs}
+                        isLoaded={isLoaded}
                     />
                     <tfoot>
                     <tr>
diff --git a/assets/js/RepLog/RepLogList.js b/assets/js/RepLog/RepLogList.js
index e0010113..be4cdbf9 100644
--- a/assets/js/RepLog/RepLogList.js
+++ b/assets/js/RepLog/RepLogList.js
@@ -2,7 +2,17 @@ import React from 'react';
 import PropTypes from 'prop-types';
 
 export default function RepLogList(props) {
-    const { highlightedRowId, handleRowClick, handleDeleteRepLog, repLogs } = props;
+    const { highlightedRowId, handleRowClick, handleDeleteRepLog, repLogs, isLoaded } = props;
+
+    if (!isLoaded) {
+        return (
+            <tbody>
+                <tr>
+                    <td colSpan="4" className="text-center">Loading...</td>
+                </tr>
+            </tbody>
+        );
+    }
 
     const handleDeleteClick = function(event, repLogId) {
         event.preventDefault();
@@ -36,5 +46,6 @@ RepLogList.propTypes = {
     highlightedRowId: PropTypes.any,
     handleRowClick: PropTypes.func.isRequired,
     handleDeleteRepLog: PropTypes.func.isRequired,
-    repLogs: PropTypes.array.isRequired
+    repLogs: PropTypes.array.isRequired,
+    isLoaded: PropTypes.bool.isRequired,
 };
