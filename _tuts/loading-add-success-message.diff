diff --git a/assets/js/RepLog/RepLogApp.js b/assets/js/RepLog/RepLogApp.js
index 50ca7e4..4c6029a 100644
--- a/assets/js/RepLog/RepLogApp.js
+++ b/assets/js/RepLog/RepLogApp.js
@@ -13,7 +13,8 @@ export default class RepLogApp extends Component {
             repLogs: [],
             numberOfHearts: 1,
             isLoaded: false,
-            isSavingNewRepLog: false
+            isSavingNewRepLog: false,
+            successMessage: ''
         };
 
         this.handleRowClick = this.handleRowClick.bind(this);
@@ -53,7 +54,8 @@ export default class RepLogApp extends Component {
 
                     return {
                         repLogs: newRepLogs,
-                        isSavingNewRepLog: false
+                        isSavingNewRepLog: false,
+                        successMessage: 'Rep Log Saved!'
                     };
                 })
             })
diff --git a/assets/js/RepLog/RepLogs.js b/assets/js/RepLog/RepLogs.js
index 66d55d1..005b6bc 100644
--- a/assets/js/RepLog/RepLogs.js
+++ b/assets/js/RepLog/RepLogs.js
@@ -26,7 +26,8 @@ export default function RepLogs(props) {
         onHeartChange,
         onDeleteRepLog,
         isLoaded,
-        isSavingNewRepLog
+        isSavingNewRepLog,
+        successMessage
     } = props;
 
     let heart = '';
@@ -46,6 +47,14 @@ export default function RepLogs(props) {
                 }}
             />
 
+            <hr/>
+
+            {successMessage && (
+                <div className="alert alert-success text-center">
+                    {successMessage}
+                </div>
+            )}
+
             <table className="table table-striped">
                 <thead>
                 <tr>
@@ -95,4 +104,5 @@ RepLogs.propTypes = {
     onDeleteRepLog: PropTypes.func.isRequired,
     isLoaded: PropTypes.bool.isRequired,
     isSavingNewRepLog: PropTypes.bool.isRequired,
+    successMessage: PropTypes.string.isRequired
 };
