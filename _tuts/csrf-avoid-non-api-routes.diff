diff --git a/src/EventSubscriber/ApiCsrfValidationSubscriber.php b/src/EventSubscriber/ApiCsrfValidationSubscriber.php
index 9eb5166..2bdbabf 100644
--- a/src/EventSubscriber/ApiCsrfValidationSubscriber.php
+++ b/src/EventSubscriber/ApiCsrfValidationSubscriber.php
@@ -14,12 +14,17 @@ class ApiCsrfValidationSubscriber implements EventSubscriberInterface
         }
 
         $request = $event->getRequest();
-        dump($request->attributes->all());die;
 
         // no validation needed on safe methods
         if ($request->isMethodSafe(false)) {
             return;
         }
+
+        if (!$request->attributes->get('_is_api')) {
+            return;
+        }
+
+        // TODO
     }
 
     public static function getSubscribedEvents()
