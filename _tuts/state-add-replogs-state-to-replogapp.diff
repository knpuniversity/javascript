diff --git a/assets/js/RepLog/RepLogApp.js b/assets/js/RepLog/RepLogApp.js
index 787ad4cd..30bef582 100644
--- a/assets/js/RepLog/RepLogApp.js
+++ b/assets/js/RepLog/RepLogApp.js
@@ -8,6 +8,11 @@ export default class RepLogApp extends Component {
 
         this.state = {
             highlightedRowId: null,
+            repLogs: [
+                { id: 1, reps: 25, itemLabel: 'My Laptop', totalWeightLifted: 112.5 },
+                { id: 2, reps: 10, itemLabel: 'Big Fat Cat', totalWeightLifted: 180 },
+                { id: 8, reps: 4, itemLabel: 'Big Fat Cat', totalWeightLifted: 72 }
+            ]
         };
 
         this.handleRowClick = this.handleRowClick.bind(this);
@@ -18,7 +23,7 @@ export default class RepLogApp extends Component {
     }
 
     render() {
-        const { highlightedRowId } = this.state;
+        const { highlightedRowId, repLogs } = this.state;
 
         let heart = '';
         if (this.props.withHeart) {
@@ -41,6 +46,7 @@ export default class RepLogApp extends Component {
                     <RepLogList
                         highlightedRowId={highlightedRowId}
                         handleRowClick={this.handleRowClick}
+                        repLogs={repLogs}
                     />
                     <tfoot>
                     <tr>
