diff --git a/Gruntfile.js b/Gruntfile.js
index f05180b..6d29ff1 100644
--- a/Gruntfile.js
+++ b/Gruntfile.js
@@ -8,7 +8,7 @@ module.exports = function (grunt) {
                 options: {
                     mainConfigFile: 'web/assets/js/common.js',
                     baseUrl: './js',
-                    appDir: 'web/assets',
+                    appDir: '<%= sourceDir %>',
                     dir: '<%= targetDir %>',
                     paths: {
                         // prevent routes from being loaded dynamically
@@ -50,10 +50,21 @@ module.exports = function (grunt) {
                 'Gruntfile.js',
                 '<%= sourceDir %>/js/**/*.js'
             ]
-        }
+        },
+
+        sync: {
+             main: {
+                 files: [{
+                     cwd: '<%= sourceDir %>',
+                     src: '**',
+                     dest: '<%= targetDir %>'
+                 }]
+             }
+         }
     });
 
     grunt.loadNpmTasks('grunt-contrib-uglify');
     grunt.loadNpmTasks('grunt-contrib-jshint');
     grunt.loadNpmTasks('grunt-contrib-requirejs');
+    grunt.loadNpmTasks('grunt-sync');
 };
diff --git a/package.json b/package.json
index 9b6466a..1748ced 100644
--- a/package.json
+++ b/package.json
@@ -14,6 +14,7 @@
     "grunt-contrib-jshint": "~0.6.3",
     "grunt-contrib-uglify": "~0.2.2",
     "grunt-contrib-requirejs": "~0.4.1",
-    "jshint-stylish": "~0.1.4"
+    "jshint-stylish": "~0.1.4",
+    "grunt-sync": "0.0.5"
   }
 }
diff --git a/web/assets/js/modules/ErrorList.js b/web/assets/js/modules/ErrorList.js
index ac2f79b..c03dcef 100644
--- a/web/assets/js/modules/ErrorList.js
+++ b/web/assets/js/modules/ErrorList.js
@@ -14,7 +14,7 @@ define(['jquery'], function ($) {
         },
 
         _toggleHidden: function() {
-            var isEmpty = $.trim(this.$wrapper.html()) == '';
+            var isEmpty = $.trim(this.$wrapper.html()) === '';
 
             this.$wrapper.toggleClass('hide', isEmpty);
         },
diff --git a/web/assets/js/modules/EventEmitter.js b/web/assets/js/modules/EventEmitter.js
index 1d69405..dd83e03 100644
--- a/web/assets/js/modules/EventEmitter.js
+++ b/web/assets/js/modules/EventEmitter.js
@@ -1,3 +1,3 @@
 define(['vendor/eventEmitter'], function (EventEmitter) {
-    return new EventEmitter()
+    return new EventEmitter();
 });
diff --git a/web/assets/js/modules/Leaderboard.js b/web/assets/js/modules/Leaderboard.js
index 95952fc..1079584 100644
--- a/web/assets/js/modules/Leaderboard.js
+++ b/web/assets/js/modules/Leaderboard.js
@@ -65,7 +65,7 @@ define(['jquery', 'dust', 'text!templates/leaderboard.dust', 'modules/Routing',
             });
 
             // fade it out
-            this.$wrapper.fadeTo(200, .4);
+            this.$wrapper.fadeTo(200, 0.4);
 
             $.ajax({
                 type: 'GET',
@@ -77,7 +77,7 @@ define(['jquery', 'dust', 'text!templates/leaderboard.dust', 'modules/Routing',
                         self.$wrapper.fadeTo(200, 1);
                     });
                 }
-            })
+            });
         },
 
         _handleUpdateLeaderboard: function(e) {
diff --git a/web/assets/js/modules/SymfonyErrorUtil.js b/web/assets/js/modules/SymfonyErrorUtil.js
index 824da17..e2804bc 100644
--- a/web/assets/js/modules/SymfonyErrorUtil.js
+++ b/web/assets/js/modules/SymfonyErrorUtil.js
@@ -19,7 +19,7 @@ define(['jquery'], function ($) {
 
             // check for global errors
             if (errorsObj.errors) {
-                errors['global'] = errorsObj.errors[0];
+                errors.global = errorsObj.errors[0];
             }
 
             $.each(errorsObj.children, function (key, data) {
