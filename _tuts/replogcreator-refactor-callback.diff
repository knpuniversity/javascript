diff --git a/assets/js/RepLog/RepLogApp.js b/assets/js/RepLog/RepLogApp.js
index 5d5bf549..9444cf43 100644
--- a/assets/js/RepLog/RepLogApp.js
+++ b/assets/js/RepLog/RepLogApp.js
@@ -27,23 +27,15 @@ export default class RepLogApp extends Component {
         this.setState({highlightedRowId: repLogId});
     }
 
-    handleNewItemSubmit(e) {
-        e.preventDefault();
-
-        const quantityInput = this.quantityInput.current;
-        const itemSelect = this.itemSelect.current;
-
+    handleNewItemSubmit(itemLabel, quantity) {
         const newRep = {
             id: uuid(),
-            reps: quantityInput.value,
-            itemLabel: itemSelect.options[itemSelect.selectedIndex].text,
+            reps: quantity,
+            itemLabel: itemLabel,
             totalWeightLifted: Math.floor(Math.random() * 50)
         };
         const newRepLogs = [...this.state.repLogs, newRep];
         this.setState({repLogs: newRepLogs});
-
-        quantityInput.value = '';
-        itemSelect.selectedIndex = 0;
     }
 
     calculateTotalWeightLifted() {
diff --git a/assets/js/RepLog/RepLogCreator.js b/assets/js/RepLog/RepLogCreator.js
index 0ae5897d..1336871b 100644
--- a/assets/js/RepLog/RepLogCreator.js
+++ b/assets/js/RepLog/RepLogCreator.js
@@ -9,25 +9,18 @@ export default function RepLogCreator(props) {
 
     function handleSubmit(e) {
         e.preventDefault();
+        console.log(quantityInput);
 
-        const quantityInput = this.quantityInput.current;
-        const itemSelect = this.itemSelect.current;
+        const itemLabel = itemSelect.options[itemSelect.selectedIndex].text;
 
-        const newRep = {
-            id: uuid(),
-            reps: quantityInput.value,
-            itemLabel: itemSelect.options[itemSelect.selectedIndex].text,
-            totalWeightLifted: Math.floor(Math.random() * 50)
-        };
-        const newRepLogs = [...this.state.repLogs, newRep];
-        this.setState({repLogs: newRepLogs});
+        handleNewItemSubmit(itemLabel, quantityInput.value);
 
         quantityInput.value = '';
         itemSelect.selectedIndex = 0;
     }
 
     return (
-        <form className="form-inline" onSubmit={handleNewItemSubmit}>
+        <form className="form-inline" onSubmit={handleSubmit}>
             <div className="form-group">
                 <label className="sr-only control-label required"
                        htmlFor="rep_log_item">
@@ -53,7 +46,7 @@ export default function RepLogCreator(props) {
                     How many times?
                 </label>
                 <input type="number" id="rep_log_reps"
-                       ref={(ref) => { quantityInput = ref }}
+                       ref={(ref) => { quantityInput = ref; }}
                        required="required"
                        placeholder="How many times?"
                        className="form-control"/>
