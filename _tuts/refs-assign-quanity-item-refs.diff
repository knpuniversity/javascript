diff --git a/assets/js/RepLog/RepLogApp.js b/assets/js/RepLog/RepLogApp.js
index f316543a..a339ee1e 100644
--- a/assets/js/RepLog/RepLogApp.js
+++ b/assets/js/RepLog/RepLogApp.js
@@ -15,6 +15,9 @@ export default class RepLogApp extends Component {
             ]
         };
 
+        this.quantityInput = React.createRef();
+        this.itemSelect = React.createRef();
+
         this.handleRowClick = this.handleRowClick.bind(this);
     }
 
@@ -26,7 +29,8 @@ export default class RepLogApp extends Component {
         event.preventDefault();
 
         console.log('I love when a good form submits!');
-        console.log(event.target.elements.namedItem('reps').value);
+        console.log(this.quantityInput);
+        console.log(this.itemSelect);
     }
 
     calculateTotalWeightLifted() {
@@ -82,7 +86,7 @@ export default class RepLogApp extends Component {
                             What did you lift?
                         </label>
                         <select id="rep_log_item"
-                                name="item"
+                                ref={this.itemSelect}
                                 required="required"
                                 className="form-control">
                             <option value="">What did you
@@ -101,7 +105,8 @@ export default class RepLogApp extends Component {
                             How many times?
                         </label>
                         <input type="number" id="rep_log_reps"
-                               name="reps" required="required"
+                               ref={this.quantityInput}
+                               required="required"
                                placeholder="How many times?"
                                className="form-control"/>
                     </div>
